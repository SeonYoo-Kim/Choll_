/** 데모용 고정 카트 ID (cart.id) — 카트 선택/로그인 기능이 생기면 라우트 파라미터로 대체 */
export const DEMO_CART_ID = 1;

/**
 * 시연 카트에 **실제로 존재하는 슬롯 수** (1번부터 이 개수까지).
 *
 * DB의 슬롯은 12개지만 실물 카트에는 RFID 리더가 5개만 달려 있어(재정상), 6번 이후 슬롯은
 * 책을 얹어도 인식되지 않는다. 적재량을 따질 때 그 슬롯까지 세면 카트는 영원히 차지 않는다.
 * 리더를 더 달면 이 값만 올리면 된다.
 */
export const PHYSICAL_SLOT_COUNT = 5;
