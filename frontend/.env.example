# 로컬 오버라이드가 필요하면 이 파일을 .env.local로 복사해서 수정 (.env.local은 커밋 금지)
VITE_ENABLE_MSW=true
# 비워두면 same-origin(/api)으로 요청하고 vite dev proxy가 BE로 중계
VITE_API_BASE_URL=
# 비워두면 ws://<현재 호스트>/ws/carts/{cartId}
VITE_WS_URL=
