#include "motor_config.h"

/* =========================================================
 * motor_config.c
 * - motor_config.h에서 extern으로 선언한 런타임 설정 변수의 정의.
 * - PI 게인은 실기 튜닝 시 Build/Flash 없이 바꿀 수 있도록 컴파일 타임
 *   매크로 대신 전역 변수로 관리한다. 기본값은 이전 매크로 값과 동일하게
 *   유지해 기존 동작(Feedforward만 동작하는 Open-loop)을 그대로 보존한다.
 * ========================================================= */
float motor_pi_kp = 0.0f;   /* 잠정값(0): 실기 튜닝 필요 */
float motor_pi_ki = 0.0f;   /* 잠정값(0): 실기 튜닝 필요 */
